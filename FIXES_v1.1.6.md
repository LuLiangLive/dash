# FIXES v1.1.6 — 上涨/下跌天数占比口径修复

> 日期：2026-08-30　|　前置：v1.1.5（弹窗指标 dwjz 口径）
> 起因：用户问「上涨天数占比和下跌天数占比，这两个数据没有问题嘛」

## 一、问题（两个叠加）

**1. 显示 bug：比例没乘 100**

后端返回 0~1 比例（016450: up_ratio=0.4118），前端三处直接 `toFixed + '%'` 渲染：

| 位置 | 代码 | 显示 |
|---|---|---|
| watch.js 自选添加弹窗 | `_st.up_ratio.toFixed(0)+'%'` | **0%** |
| watch.js 对比表（pct_ratio 分支） | `v.toFixed(1)+'%'` | **0.4%** |
| fund-detail.js 抗跌弹窗 | `st.up_ratio.toFixed(1)+'%'` | **0.4%** |

同表里「上涨捕获率」乘了 100（41.2%）、「上涨天数占比」没乘（0.4%）——并排出现，一眼穿帮。

**2. 阈值语义 bug：±0.3% 才算涨跌**

`ranker.calc_metrics` 里 up_ratio 用 `> 0.3%`、dn_ratio 用 `< -0.3%`，而同函数里 up7/dn7（近7日）用的是 `> 0` —— 同一页面两个"上涨天数"口径自相矛盾；且对低波动基金（债基等）绝大多数日子在 ±0.3% 内，占比趋近 0，看着"从不涨"。

## 二、修复（后端统一，前端零改动）

| 文件 | 修改 |
|---|---|
| `collector/ranker.py` | 阈值 `±0.3%` → `>0 / <0`，与 up7/dn7 对齐（仅展示字段，不参与评分） |
| `night_fund_monitor.py` | watch 弹窗输出 `up_ratio/dn_ratio` ×100（round 1 位） |
| `main.py` anti 弹窗 + 双基金对比 | 同样 ×100（含 summary 文案 `%.0f%%` 自动变正确） |

三个弹窗/对比接口输出统一为**百分比**，与 `stats_full`（fund_detail.py 本就 ×100）口径一致。

## 三、验证（修复后实测）

| 基金 | up_ratio | dn_ratio | up+dn | 修复前显示 |
|---|---|---|---|---|
| 016450 | **47.1%** | **52.9%** | 100.0% | 0% / 0% |
| 161725 | **44.5%** | **53.8%** | 98.3% | 0% / 0% |
| 017856 | — | — | 99.2% | 0% / 0% |

- watch 弹窗与 anti 弹窗数值一致（47.1/52.9）
- 150 项测试全绿（4.79s）
- 该字段仅展示用，不参与评分/榜单，改口径无排名影响

## 四、遗留说明

- `funds` 表（rank_full 写入）的 up_ratio 仍存 0~1 比例 —— 无任何前端/API 直接消费该字段，仅作存储与审计，未动。
- `dn_ratio` 此前曾被字段审计标记为"僵尸字段"（meta 层），但弹窗前端仍在使用，属正常展示字段。
