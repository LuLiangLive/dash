# FIXES v1.1.4 — 取数提速与冗余数据治理（弹窗 + 一键更新）

> 日期：2026-08-30　|　前置：v1.1.3（016450 涨幅/榜单修复）
> 背景：用户逐项确认的四项优化 + 实测中发现并修正的一处错误分类
> 全部改动仅涉及 `collector/fetcher.py` 与 `analysis_pipeline/night_fund_monitor.py`

---

## 一、四项确认修改

### ① `_fetch_name` 不再裸打 pingzhongdata（弹窗）

- **改动前**：`night_fund_monitor._fetch_name` 直接 `fetcher._get("http://fund.eastmoney.com/pingzhongdata/{code}.js")`，**绕过全部缓存**，每次弹窗都重新下载 ~129KB 完整 js，只为提取一个基金名。
- **改动后**：改走 `fetcher.fetch_pingzhong(code, need=("name",))` —— 复用 6h 档案缓存（`_PZ_CACHE`）、15 分钟原始文本缓存（`_PZ_RAW`）与单飞锁。
- **收益**：弹窗重复场景 0 网络流量；首次场景与其他弹窗分支共享同一份响应。

### ② `fetch_basic` 24h LRU 缓存（弹窗）

- **改动前**：jbgk 概况页（45.7KB / 0.32s）每次弹窗都重新下载，而规模/成立日期是月度级变化的字段。
- **改动后**：新增 `_BASIC_CACHE`（TTL 24h，容量 600），只缓存成功结果，失败下次重试。
- **收益**：24 小时内同一基金的概况查询 0 网络。

### ③ pingzhongdata 单飞锁（singleflight）

- **改动前**：同一基金的并发 `fetch_pingzhong`（典型场景：一键更新与弹窗同时触发）会**并发下载两份 129KB**。
- **改动后**：按 code 粒度 `threading.Lock`（`_pz_lock`），只放一个线程真正走网络；其余线程等锁后**双检两级缓存**直接命中返回。
- **收益**：并发场景流量减半；锁注册表 ~1 万只 ≈ 1MB 内存，可接受。

### ④ 一键更新增量抓取改为「按缺口区间」（核心）

- **改动前**：`fetch_nav_incremental` 对所有待补抓基金统一 `fetch_nav_history(page_size=60)`（3 页请求、60 条数据），不管实际缺多少。
- **改动后**（新增 `nav_cached_max_many` 批量查询每只基金缓存最新日期，6092 只实测 66ms）：

| 基金状态 | 策略 | 请求页数 | 数据量 |
|---|---|---|---|
| 缓存健康（最新日=目标日 且 近30天≥15点） | **跳过** | 0 | 0 |
| 无缓存（新基金首抓） | 近窗 60 条（保弹窗 40 条门槛） | 3 | ~60 条 |
| 缓存落后（缺口 1~8 周） | `fetch_nav_range(cached_max+1, 目标日)`，封顶 2 页 | 1~2 | **仅缺口条数** |
| 缓存已到目标日但密度不足（历史 NULL） | 近窗 60 条重抓，由「只补不砸」COALESCE upsert 补 NULL | 3 | ~60 条 |

- **实测**（伪装缺口落后 5 个交易日）：三只基金各只拉回 **5 条**（08-24 ~ 08-28），旧逻辑同样场景要拉 60 条 —— **每只基金省 2 次请求 + ~55 条冗余**。
- 长缺口场景逐轮收敛：每轮最多前推 40 条（≈8 周），`cached_max` 持续前移。

## 二、实测中发现并修正的错误分类（新增）

④ 上线后首次一键更新报「**失败 63 只**」，排查结论：

- 这 63 只全部是**基金代码库（universe）里的慢发布基金**（如 QDII T+2）：净值停在 08-27，目标日 08-28 尚无新净值可拉。
- 旧逻辑抓 60 条回窗能拿到"旧数据"所以不报错（**掩盖了真相**）；新逻辑按缺口只拉 08-28 一天 → 空返回 → `pipeline.py:447` 把 `navs_all` 为空误判为「无净值数据」。
- **数据全程无损、榜单无影响**（它们不在 funds 表）；但错误面板在说谎。
- **修正**（`fetcher.fetch_nav_incremental.work()`）：缺口抓取空返回 **且** 库内有缓存（`cm is not None`）→ 归入 `skipped`（带 `cached_max`），合并阶段直接读库算指标；仅「首抓即空」的真死代码才计入失败。
- 修正后复测：**失败 0 只**，且 6034 只全部完成指标计算（修正前 5971）。

## 三、验证记录（2026-08-30 凌晨实测）

| 检查项 | 结果 |
|---|---|
| 测试套件 | **150 passed in 4.78s**（与基线一致） |
| `nav_cached_max_many` 全量 | 6092 只批量查询 66ms，无缓存代码正确缺席 |
| 缺口路径（伪装落后 5 交易日） | 每只仅拉 5 条 / 1 页请求（旧逻辑 60 条 / 3 页） |
| 一键更新（第 1 次，修正前） | 6034 池 → 跳过 5954 → 实抓 80 → **37.0s**，暴露「失败 63 只」假报错 |
| 一键更新（第 2 次，修正后） | 6034 池 → 跳过 6017 → **32.9s · 失败 0 只** |
| 弹窗冒烟 `/api/watch/fetch?code=016450` | 冷 0.268s 数据完整；热 0.0025s |
| 弹窗冒烟 `/api/fund/anti_detail` | 冷 0.013s；热 0.0025s；两次结果一致 |
| 当日榜一致性 | 016450 **#10 / 2.1615%**、017856 #14 / 2.0793%（真值 2.08 ✓） |
| 榜首真值核对 | 018049：1.0649→1.0997 = 3.2679% 与榜单完全一致 |
| 数据完整性 | 近 30 天 NULL dwjz 占比 0.33%（基线 0.4%，无回归）；nav_history 418,963 行 |

## 四、弹窗页请求预算对比（单只基金首次弹窗）

| 分支 | 改动前 | 改动后 |
|---|---|---|
| 名称 `_fetch_name` | 129KB（每次必打） | **0**（与其他分支共享/缓存命中） |
| 概况 `fetch_basic` | 45.7KB × 2 尝试 | **0**（24h 内） |
| pingzhongdata 主请求 | 129KB × 可能 2 次（并发重复） | **129KB × 1**（单飞锁） |
| `_long_series` 全史 | 129KB（已有，落库） | 129KB（不变，唯一高价值长数据源） |

并发去重后，弹窗总流量从 ~433KB（最坏情况）降至 **~258KB**；重复弹窗接近 0 流量。

## 五、遗留观察项（未改动，仅记录）

1. **63 只慢发布基金每轮都会进 fetch_list**：缺口抓取 1 页请求后归入 skipped，代价是每轮 63 次轻量请求；如需进一步省流量可加「慢发布负缓存」（记录最近一次空返回时间，24h 内跳过）。
2. pytest 进程退出时被 `db.py` 的 aiosqlite 非守护线程拖住（既有问题，测试结果不受影响）。
3. `nav_is_healthy` 对「成立不满 30 天的新基金」永远判不健康 → 每轮重抓近窗；新基金数量极少，暂不值得特判。
