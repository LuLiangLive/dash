# 投研看板 — 部署状态

> 版本：完整部署包 v1.1.6（弹窗涨跌天数占比口径修复）
> 部署时间：2026-08-29
> 最近更新：2026-08-30（累计 12+4+1+1+1 项修复，详见 `DEPLOY_AUDIT.md`、`FIXES_v1.1.3.md`、`FIXES_v1.1.4.md`、`FIXES_v1.1.5.md`、`FIXES_v1.1.6.md`）

## 〇·〇、v1.1.5 核心修复（弹窗指标口径）

| 指标 | 修复前（ljjz 累计净值口径） | 修复后（dwjz 单位净值口径，与榜单/东财一致） |
|---|---|---|
| 161725 最新净值 | 2.2682（累计） | **0.5521** |
| 161725 d1 | 0.2564% | **1.0617%** |
| 161725 max_dd | -7.49% | **-26.54%** |
| 161725 vol | 5.99% | **24.25%** |
| m1/m3/m6/y1 | 部分被净值计算值覆盖 | **东财 syl_* 官方值优先**（-0.93/-2.95/-19.45/-31.66） |

影响面：全市场 **822 只** 有分红/拆分历史的基金。自选弹窗、抗跌弹窗、双基金对比、极端回撤警示共 7 处消费点统一 dwjz 口径。150 项测试全绿，7 只抽样弹窗 d1 与真值全部一致。详见 `FIXES_v1.1.5.md`。

## 〇、v1.1.4 核心优化（取数提速与冗余数据治理）

| # | 改动 | 收益 |
|---|---|---|
| ① | `_fetch_name` 改走统一缓存入口 | 弹窗不再每次裸打 129KB pingzhongdata |
| ② | `fetch_basic` 24h LRU | 概况页 45.7KB/0.32s → 24h 内 0 网络 |
| ③ | pingzhongdata 单飞锁 | 并发场景同基金只下载一份 |
| ④ | 增量抓取按缺口区间（封顶 2 页） | 缺口 5 个交易日：60 条/3 页 → **5 条/1 页** |
| ⑤ | 缺口空返回重分类为 skipped | 一键更新假报错「失败 63 只」→ **失败 0 只** |

实测：一键更新 6034 只 **32.9s · 失败 0 只**；150 项测试全绿；016450 当日榜 #10 / 2.1615% 保持正确。详见 `FIXES_v1.1.4.md`。

## 〇·一、v1.1.3 核心修复（016450 单日涨幅 & 当日榜排名）

| 项目 | 修复前 | 修复后 | 网络真值 |
|---|---|---|---|
| 016450 d1 | 3.3603%（错，跨5个交易日） | **2.1615%** | 2.16% ✓ |
| 016450 当日榜排名 | **#1（错误）** | **#10** | — |
| 017856 d1 | 2.6127%（脏） | **2.0793%** | 2.08% ✓ |
| 021087 / 021146 d1 | 8.76% / 3.95%（脏） | 1.60% / 1.50% | 1.60% / 1.50% ✓ |

根因 3 层：种子数据 dwjz 仅周五有值且 ljjz 错位一天 → `cached_nav_max` 只判 ljjz 导致永久跳过 → `_MEMO_METRICS` 缓存永不失效导致榜单沿用旧值。
修复 5 处代码 + 定向区间补数（53+28 只，24 秒）+ 清理非交易日空行 36,474 条。详见 `FIXES_v1.1.3.md`。

## 一、云端部署（公网可访问）

**分享链接：https://ab7d0e0ddbc50ef12.app.workbuddy.link**

| 项目 | 地址 |
|---|---|
| 看板首页 | https://ab7d0e0ddbc50ef12.app.workbuddy.link/ |
| 接口文档 | https://ab7d0e0ddbc50ef12.app.workbuddy.link/docs |
| 健康检查 | https://ab7d0e0ddbc50ef12.app.workbuddy.link/api/health |

### 最近一次发布：2026-08-30 02:20（v1.1.6，涨跌天数占比口径修复）

云端验证结果（v1.1.6 发布后实测）：

| 检查项 | 结果 |
|---|---|
| 健康检查 | **200** |
| 016450 watch 弹窗 | up_ratio **47.1%** / dn_ratio **52.9%**（修复前显示 0%/0%） |
| 161725 anti 弹窗 | up_ratio **44.5%** / dn_ratio **53.8%** |
| 016450 当日榜 | **#10 / 2.1615%**（无回归） |

根因 2 层：后端返回 0~1 比例被前端直接拼 `%`（未 ×100）；阈值 ±0.3% 与 up7/dn7 的 >0 口径自相矛盾。修复：ranker 阈值对齐 >0，三处 API 输出统一 ×100（前端零改动）。详见 `FIXES_v1.1.6.md`。

> 历史记录：v1.1.5（02:14，弹窗指标 dwjz 口径）· v1.1.4（01:58，取数提速）· v1.1.2（2026-08-29 23:37）。

### 云端部署要点
- 以**单端口 HTTP 服务**发布，入口 `run_server.py`（读取平台注入的 `PORT`，绑定 `0.0.0.0`）
- 技术栈：FastAPI + SQLite（本地文件库，无外部依赖服务）
- 重复发布会覆盖同一链接的内容，对所有持有链接的人立即生效

### 云端 API 密钥
- 已配置强随机密钥：`zX5S8bBf5sghyuWSN7tMeCyU4ot6nWL8nJksNkOzgb0`
- 写接口（`POST/PUT/DELETE /api/watchlist`）需请求头 `X-API-Key`
- **读接口保持开放**（有意为之）：前端请求不带 Key，若启用 `REQUIRE_READ_KEY=1` 会导致公开看板白屏

### 注意
- 云端使用**发布时刻**的 `fund.db` 快照；本地采集器的后续更新**不会自动同步**到云端，需要时重新发布即可
- 云端仅运行 API 服务；采集调度器属后台常驻任务，如需云端定时采集请使用支持后台 Worker 的平台（如 CloudBase 云托管）

---

## 二、本地部署（沙箱内）

| 角色 | 命令 | PID | 说明 |
|---|---|---|---|
| API 服务 | `API_KEYS=… python3.11 main.py 8000` | 803560 | 监听 `0.0.0.0:8000` |
| 采集调度器 | `python3.11 -u collector/schedule.py` | 803561 | 后台定时任务 |

- 本地访问：http://localhost:8000/
- 数据库：`/workspace/invest-dashboard/fund.db`（6039 只基金，净值历史 418,963 条，近30天 NULL dwjz 占比 0.33%）
- 定时任务：基金采集每日 `20:30`，资讯采集 `07:00 / 12:00 / 20:00`（北京时间）
- 日志：`/tmp/server.log`（API）、`/tmp/collector.log`（采集器）

### 一键更新实测（v1.1.4 取数优化后 2026-08-30 01:49 实测）

| 项目 | v1.1.3（00:08 实测） | v1.1.4（01:49 实测） |
|---|---|---|
| 结果 | ✅ 失败 0 只 | ✅ **失败 0 只**（且修复了中间版本的「失败 63 只」假报错） |
| 缓存命中 | 5,954 只跳过 / 80 只补抓 | **6,017 只跳过** / 17 只补抓 / 63 只慢发布归入跳过 |
| 全程用时 | — | **32.9s** |
| 指标计算覆盖 | 5,971 只 | **6,034 只**（全池） |
| 增量抓取口径 | 待补抓一律 60 条/3 页 | **按缺口区间**（封顶 2 页），缺口 5 交易日实测仅 5 条/1 页 |
| 榜单一致性 | 016450 当日榜 #10 / 2.1615% | 保持一致；榜首 018049 真值核对 3.2679% ✓ |

### 四个状态卡片（修复后实测）

```
已更新基金   6039/6039   今日
下次计划     08-30 20:30  每日
最新净值日   2026-08-28  T+1
最新榜单日   2026-08-30  已生成
```

### 常用运维命令
```bash
# 健康检查
curl -s http://localhost:8000/api/health

# 查看实时日志
tail -f /tmp/server.log
tail -f /tmp/collector.log

# 停止本地服务
kill 803560 803561

# ⚠️ 重新打包后必须重启本地服务：打包脚本会以快照覆盖 fund.db,
# 而 db.py 是线程级持久连接, 旧进程会一直持有旧 inode, 造成新旧文件分叉。

# 重启 API（务必带上 API_KEYS）
cd /workspace/invest-dashboard
export API_KEYS=zX5S8bBf5sghyuWSN7tMeCyU4ot6nWL8nJksNkOzgb0
nohup python3.11 main.py 8000 > /tmp/server.log 2>&1 &

# 重启采集器
nohup python3.11 -u collector/schedule.py > /tmp/collector.log 2>&1 &

# 同步最新数据/代码到云端（会覆盖线上内容）
node /root/.codebuddy/skills/发布为应用/scripts/publish.js --dir /workspace/invest-dashboard --language python --start-cmd "python3 run_server.py"
```

---

## 三、已应用的修复

详见 `DEPLOY_AUDIT.md`。共修复 3 项：

1. **`api/watch.py` 路由装饰器错位** — 装饰器误装饰内部函数，导致正式实现为死代码、接口返回畸形元组 `[{...}, null]`
2. **默认密钥可被任意使用** — 生成强随机密钥替换 `dev-key-123`
3. **`/api/watch/sync` 一直返回 500** — `main.py:91` 补导入 `watchlist_sync`（此前前端自选从不与服务端同步，且被 try/catch 静默吞掉）

回归测试：150 项全部通过。

## 四、备选：Docker 部署（官方推荐双容器）

项目自带 `Dockerfile` 与 `docker-compose.yml`（API + 采集器双容器）：
```bash
cd /workspace/invest-dashboard
docker compose up -d
docker compose ps
docker compose logs -f api
```
需挂载 `./data` 持久化卷，否则容器重启后采集数据丢失。

## 五、环境变量
| 变量 | 默认值 | 说明 |
|---|---|---|
| `PORT` | 云端注入 / 本地 8000 | 服务端口（`run_server.py` 读取） |
| `DB_PATH` | ./fund.db | SQLite 路径 |
| `API_KEYS` | 强随机密钥 | API 密钥（写操作校验），未注入时 `run_server.py` 设置安全默认值 |
| `REQUIRE_READ_KEY` | 0 | 读操作是否也需 Key（启用会导致无 Key 的前端白屏，公网慎用） |
| `FULL_MARKET` | 1 | 全市场采集 |
| `COLLECT_MAX` | 6000 | 全量采集基金数上限 |
| `TZ` | Asia/Shanghai | 时区 |
