# FIXES v1.1.5 — 弹窗指标口径修复（累计净值 → 单位净值）

> 日期：2026-08-30　|　前置：v1.1.4（取数提速与冗余治理）
> 起因：用户反馈「弹窗的指标那些数据有问题」
> 根因：弹窗/详情层全部指标按 **ljjz（累计净值）** 计算，而榜单（v1.1.3 已修）与东方财富均按 **dwjz（单位净值）** —— 全市场 **822 只** 有分红/拆分历史的基金两种口径严重偏离。

---

## 一、问题实测（修复前，161725 招商中证白酒）

| 指标 | 弹窗（ljjz 口径） | 真值（dwjz 口径 / 东财） | 偏差 |
|---|---|---|---|
| 最新净值 | **2.2682**（累计） | 0.5521 | **4.1 倍** |
| d1 当日涨幅 | 0.2564% | **1.0617%** | -0.81pp |
| d2 两日涨幅 | 0.0971% | 0.4001% | -0.30pp |
| max_dd 最大回撤 | -7.49% | **-26.54%** | 分红跳水被 ljjz 平滑 |
| vol 年化波动 | 5.99% | **24.25%** | 严重低估 |
| m1 近1月 | -1.40%（计算值） | **-0.93%**（东财 syl_*） | 官方值被覆盖 |

分红/拆分基金的全市场影响面：**822 只**（最新净值日 ljjz ≠ dwjz，偏差最高 310%）。

## 二、修复内容（弹窗/详情层统一 dwjz 口径，与榜单一致）

新增工具 `collector/fetcher.nav_pairs(items, min_pts=8)`：取 (date, dwjz) 升序对；dwjz 过滤后不足 8 点才整体回退 ljjz（**两种口径不混用**，避免切换点假跳变）。

| 文件 | 修改点 | 说明 |
|---|---|---|
| `collector/fetcher.py` | 新增 `nav_pairs` | 共用口径工具 |
| `night_fund_monitor.py` | `fetch_watch_detail` 净值序列构建 | 自选弹窗全部指标改 dwjz；nav 显示单位净值 |
| `night_fund_monitor.py` | `calc_metrics` / `anti_metrics` dict 适配 | `dwjz or ljjz` 优先取 |
| `night_fund_monitor.py` | m1/m3/m6/y1 改 **setdefault** 语义 | 修复「syl_* 官方值被后续无条件重算覆盖」（实测 161725 近1月显示 -5.49% 而官方 -0.93%） |
| `main.py` | `_series` / `_long_series` 序列构建 | 抗跌弹窗/双基金对比/近6月近1年回撤全部改 dwjz |
| `main.py` | `_calc_merge` / `_anti_detail_sync` / 弹窗 anti_metrics 传参 | 传 dwjz 键 |
| `main.py` | `_extreme_warn_tag` 单日跌幅 | 极端回撤警示改 dwjz 口径 |

**未动的部分**：指数序列（`_kc_series` / `_bench_series` / `_LIVE_CTX`）保持 ljjz —— 指数无分红概念，ljjz 口径本就正确；`/api/funds/{code}/nav` 原样返回双键行（前端自由取用）。

## 三、验证结果（修复后实测）

| 检查项 | 结果 |
|---|---|
| 测试套件 | **150 passed in 4.77s** |
| 161725 弹窗 | nav=0.5521 ✓ d1=1.0617% ✓ d2=0.4001% ✓ max_dd=-26.54% ✓ vol=24.25% ✓ |
| 161725 m1/m3/m6/y1 | -0.93 / -2.95 / -19.45 / -31.66（官方 syl_*）✓ |
| 7 只抽样（含 4 只分红基金）d1 | 弹窗值 == dwjz 真值，全部 ✓（误差 <0.02pp） |
| 016450 / 017856 对照 | 2.1615% / 2.0793% 无回归 ✓ |
| 抗跌弹窗 stats | mdd/vol/ret2m 与自选弹窗一致（同 dwjz 口径）✓ |
| 当日榜 | 016450 #10 / 2.1615% 不变，榜首 018049 / 3.2679% ✓ |

## 四、遗留观察项

1. `dd20`（距 20 日高点）在弹窗中依赖大跌日列表（ddown），非榜单基金（funds 表无 dd20 行）时为 None —— 既有行为，非本次引入。
2. 自选弹窗 m1/m3 的「计算值兜底」仅在 `fetch_fund_returns`（syl_*）失败时生效，属于正常降级路径。
