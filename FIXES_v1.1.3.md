# v1.1.3 修复记录 —— 016450 单日涨幅错误 & 当日榜排名错误

## 一、问题现象

1. **016450（南方新材料股票发起C）单日涨幅显示 +3.36%**，而真实值应为 **+2.16%**（接口 `JZZZL=2.16` 为权威口径）。
2. **016450 因此错误排到「当日榜」第 1 名**（真实排名应约 #10）。

## 二、根因分析（三层叠加）

### 层1：种子数据本身是脏的（数据层）
016450 的 `nav_history` 里 805 行中有 792 行 `dwjz` 为 NULL，**ljjz 整体错位一天**：
```
2026-08-28  ljjz=1.3565  dwjz=1.3565   ← 正确
2026-08-27  ljjz=1.3565  dwjz=NULL     ← ljjz 实际是 08-28 的值(错位)
2026-08-26  ljjz=1.3278  dwjz=NULL     ← 实际是 08-27 的值
...
```
所有指标（`_ret` / `calc_metrics` / `composite_score`）都用 **dwjz** 计算。过滤掉 NULL 后，相邻两个有效点相隔 **5 个交易日**（种子按周填充，仅周五有值），于是 `_ret(navs, 1)` 实际算的是**跨一周的涨幅**：
```
(1.3565 / 1.3124 - 1) × 100 = 3.36%   ← 错的(跨 5 个交易日)
(1.3565 / 1.3278 - 1) × 100 = 2.16%   ← 对的(真实单日)
```

### 层2：`cached_nav_max` 只按 ljjz 判断（缓存判定层）
```
修复前: SELECT MAX(date) FROM nav_history WHERE code=? AND ljjz IS NOT NULL
修复后: SELECT MAX(date) FROM nav_history WHERE code=? AND dwjz IS NOT NULL
```
016450 每周五仍会写入一条带 dwjz 的行，所以旧的 `MAX(ljjz日期)` 恒等于目标日期 →
增量逻辑判定「缓存已最新」**永久跳过**这只基金，脏数据永远不会被刷新。

### 层3：`_MEMO_METRICS` 进程级缓存永不失效（榜单层）
`rank_full.py` 的 `_metrics_for()` 把每只基金的指标缓存在模块级 dict 里，**进程不重启就永远复用**。
一键更新在同一进程内重抓了净值、重写了 `nav_history`、刷新了 `funds` 表，
但重排榜单时 `_metrics_for` 直接命中旧缓存 → 榜单 meta 里写的还是旧 d1=3.36%。

### 附带发现：降级数据回写污染（数据安全层）
`local_nav_fallback()` 读 `nav_cache.json`（种子按周填充数据）兜底算指标，
但 pipeline 把兜底结果也 `bulk_upsert_navs` 回写进了 `nav_history` ——
实测导致补数完成的 **017856 又被写回 266 行 dwjz=NULL**、d1 重新变错。

## 三、修复内容（5 处）

| 文件 | 修复 |
|---|---|
| `collector/fetcher.py` | ① `cached_nav_max` 改按 `dwjz IS NOT NULL` 判最新日期<br>② 新增 `fetch_nav_history(start_date/end_date)` 区间参数 + `fetch_nav_range()` 便捷封装（原来 `startDate=`/`endDate=` 被硬编码为空串，补 1 天数据也得翻完整历史）<br>③ 新增 `nav_is_healthy()` / `_batch_nav_health()`：缓存「最新日期对上 **且** 近30天 dwjz 点数 ≥ 15（全市场中位数 22，脏数据仅 4~5）」，增量跳过判定从"只看日期"升级为"日期+密度"双条件 |
| `collector/rank_full.py` | 新增 `clear_metrics_cache()`，`compute_all_panels()` 开头强制清空 `_MEMO_METRICS`，保证重排榜单永远用本轮净值现算 |
| `collector/pipeline.py` | `local_nav_fallback` 兜底结果只用于算指标，**不再回写** `nav_history`（`_from_fallback` 标记） |
| `db.py` | `bulk_upsert_navs` 冲突更新从 `dwjz=excluded.dwjz`（无条件覆盖）改为 `dwjz=COALESCE(excluded.dwjz, nav_history.dwjz)`（**只补不砸**，NULL 不能抹掉已有好数据） |
| 数据修复脚本 | `/tmp/repair_nav_range.py`：只对受影响基金、只拉最近 400 个自然日区间（覆盖 `limit=252` 全部指标窗口），并发 8 线程，53 只 **24 秒** 完成 |

## 四、数据修复执行结果

- 补数 53 只（首次）+ 28 只（二次，含被回写污染的 017856），均 **0 失败**
- 全量清理种子伪造的**非交易日空行** 36,474 条（用"全市场有 dwjz 的日期"反推交易日历）
- `nav_history`：418,963 行，dwjz 非空 417,079 行（99.6%）
- 全市场近 30 天 dwjz 点数 < 15 的基金仅剩 4 只（均为**真实按周披露**的新基金，如 027300）

## 五、验证结果

| 项目 | 修复前 | 修复后 | 真值 |
|---|---|---|---|
| 016450 d1 | 3.3603% | **2.1615%** | 2.16% ✓ |
| 016450 当日榜排名 | **#1（错误）** | **#10** | — |
| 017856 d1 | 2.6127%（脏） | **2.0793%** | 2.08% ✓ |
| 021087 d1 | 8.7637%（脏） | 1.6049% | 1.60% ✓ |
| 021146 d1 | 3.9549%（脏） | 1.5020% | 1.50% ✓ |
| 榜单 meta.d1 与 funds.d1 | 不一致（缓存） | 一致 | — |
| 首页 `****` | 0 | 0 | — |
| 一键更新失败数 | 39 → 0 | **0** | — |

当日榜 TOP3（2026-08-30）：018049 3.27% / 023602 2.94% / 017736 2.61%，
抽查 TOP10 与接口 `JZZZL` 交叉校验 **9/10 精确一致**。

## 六、关键经验

1. **算涨幅只需要确定日期区间的净值**，不需要拉全量历史 ——
   `startDate`/`endDate` 是接口原生参数，之前被硬编码为空是补数慢的根因。
2. 指标计算一律用 `dwjz`，判定"缓存是否最新"也必须用 `dwjz`（否则漏判）。
3. 任何进程级缓存，只要数据可能被同进程内更新，就必须有失效点。
4. 入库冲突更新应该「只补不砸」：NULL/0 永远不应该覆盖已有有效值。
